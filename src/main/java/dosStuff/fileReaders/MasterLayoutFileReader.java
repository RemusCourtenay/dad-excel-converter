package dosStuff.fileReaders;

import dosStuff.FileIOThreadManager;

public class MasterLayoutFileReader extends FileReader {

    public MasterLayoutFileReader(FileIOThreadManager fileIOThreadManager) {
        super(fileIOThreadManager);
    }

}
